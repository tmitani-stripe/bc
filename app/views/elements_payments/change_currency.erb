function changeCurrency(new_currency, price) {
    console.log(price);
    const targetPriceText = document.getElementById(`main-currency-total`);
    targetPriceText.innerHTML = `合計額: ${price}`;
}
changeCurrency("<%= @order.payment_currency %>", "<%= @price %>");